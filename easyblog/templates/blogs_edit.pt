<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:tal="http://xml.zope.org/namespaces/tal"
      xmlns:metal="http://xml.zope.org/namespaces/metal"
      metal:use-macro="base">
  <tal:block metal:fill-slot="content">
    <div class="row">
      <div class="page-header span4">
        <h1 i18n:translate="blogs_topic"> Edit list of Blogs </h1>
      </div>
      <span tal:condition="provider" tal:replace="structure provider('editbar')" />
    </div><!-- row -->
    ${message}
    ${form.begin(request.url)}
    ${form.csrf_token()}
    <div class="row">
        <table class="table span8">
          <thead>
            <th> Name </th>
            <th> Creator </th>
            <th> Date </th>
          </thead>
          <tbody>
            <div tal:repeat="blogs_item context.order_by_time()">
              <tr>

                <!-- Title -->    
                <td>
                  <a tal:content="blogs_item.title" 
                   tal:attributes="href resource_url(blogs_item, request)">
                   News
                  </a>
                </td>

                <!-- Creator -->    
                <td tal:content="blogs_item.owner" >

                <!-- Date -->    
                <td tal:content="blogs_item.date()">News</td>

                <td>
               <input tal:attributes="value blogs_item.id;
                        name string:checkbox-${blogs_item.id}"
                        class="checkbox" type="checkbox"/>
                </td>
              </tr>
            </div>
          </tbody>
        </table>
    </div><!-- row -->
    <button i18n:translate="delete" class="btn" type="submit" name="submit" value="Delete">Delete</button>
    ${form.end()}
  </tal:block>
</html>
